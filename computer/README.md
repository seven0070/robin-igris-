# Carry Computer

Vibe UI for Robin Igris / PNAOS — a portable chassis where **the agent is the shell**.

```bash
cd computer && python3 -m http.server 8765
# open http://127.0.0.1:8765
```

Power on → boot → talk to Robin or use `:help` / `:status` / `:soul` / `:sleep`.
